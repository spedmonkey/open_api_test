{
    "swagger": "2.0",
    "basePath": "/",
    "paths": {
        "/hello/": {
            "get": {
                "responses": {
                    "200": {
                        "description": "Success"
                    }
                },
                "summary": "Fetch a greeting message",
                "operationId": "get_hello_world",
                "tags": [
                    "hello"
                ]
            }
        }
    },
    "info": {
        "title": "Simple API",
        "version": "1.0",
        "description": "A simple API"
    },
    "produces": [
        "application/json"
    ],
    "consumes": [
        "application/json"
    ],
    "tags": [
        {
            "name": "hello",
            "description": "Hello operations"
        }
    ],
    "responses": {
        "ParseError": {
            "description": "When a mask can't be parsed"
        },
        "MaskError": {
            "description": "When any error occurs on mask"
        }
    }
}
